import { Fun } from 'types/Functions'
import { STORAGE } from '../../index';

export default {
    name: 'loops',
    description: 'Loop functions',

    methods: [
        {
            name: 'for',
            description: 'For loop',
            usage: 'for(10) / for(i = 5 i < 10) / for([elements]',
            run: () => {

                // @TODO create this

            }
        }
    ]

} as Fun