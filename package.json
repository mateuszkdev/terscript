{
    "name": "typescript-interpreter",
    "version": "2.0.0",
    "description": "Simple interpreter written in TypeScript",
    "main": "build/index.js",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "dev": "concurrently \"tsc --watch\" \"nodemon . --dev true --file test\""
    },
    "author": "Mateusz4711 (https://github.com/mateuszkdev)",
    "license": "ISC",
    "devDependencies": {
      "@types/node": "^20.5.1",
      "concurrently": "^8.2.0"
    }
  }